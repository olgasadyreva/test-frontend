import React from 'react';
import FormAuth from '../FormAuth/FormAuth';

const Home = () => {
    return (
        <FormAuth/>
          /* <FormAuth isAuth={this.state.isAuth}/> */
    )
}

export default Home;